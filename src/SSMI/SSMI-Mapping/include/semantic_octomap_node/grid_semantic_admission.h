#ifndef SEMANTIC_OCTOMAP_GRID_SEMANTIC_ADMISSION_H
#define SEMANTIC_OCTOMAP_GRID_SEMANTIC_ADMISSION_H

#include <semantic_octomap_node/semantic_schema.h>

// Backward-compatible include path. Semantic admission policy now comes from
// params/semantic_schema.yaml through SemanticSchema; no palette or label range
// is compiled into this header.

#endif  // SEMANTIC_OCTOMAP_GRID_SEMANTIC_ADMISSION_H
